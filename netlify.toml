[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "22"

# Netlify sets NETLIFY=true during builds — vite.config.ts uses @netlify/vite-plugin-tanstack-start.
